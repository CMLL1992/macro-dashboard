{
  "installCommand": "npm_config_build_from_source=true pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/warmup", "schedule": "0 0 * * *" }
  ]
}

