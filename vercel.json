{
  "buildCommand": "pnpm approve-builds esbuild better-sqlite3 unrs-resolver || true && pnpm run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/warmup", "schedule": "0 */3 * * *" }
  ]
}

