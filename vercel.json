{
  "installCommand": "pnpm approve-builds esbuild better-sqlite3 unrs-resolver && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/warmup", "schedule": "0 0 * * *" }
  ]
}

